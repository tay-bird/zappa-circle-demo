version: 2.1

orbs:
  zappa: borb/zappa@dev:master

workflows:
  deploy-demo:
    jobs:
      - zappa/zappa-deploy:
          stage: staging
          filters:
            branches:
              only: master
